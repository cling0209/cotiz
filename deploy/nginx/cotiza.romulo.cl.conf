# cotiz — instancia Rómulo
# Proxy: 127.0.0.1:8001

server {
    listen 80;
    listen [::]:80;
    server_name cotiza.romulo.cl;

    location / {
        proxy_pass http://127.0.0.1:8001;
        include /etc/nginx/snippets/laravel-proxy.conf;
    }
}
