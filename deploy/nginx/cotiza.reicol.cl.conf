# cotiz — instancia Reicol
# Proxy: 127.0.0.1:8002

server {
    listen 80;
    listen [::]:80;
    server_name cotiza.reicol.cl;

    location / {
        proxy_pass http://127.0.0.1:8002;
        include /etc/nginx/snippets/laravel-proxy.conf;
    }
}
